<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.c4.routy.domain.user.mapper.AuthMapper">
    <select id="findEmailByUsernameAndPhone" parameterType="string">
        SELECT
               EMAIL
          FROM tbl_user U
         WHERE U.USERNAME = #{username}
           AND U.PHONE = #{phone}
    </select>

</mapper>